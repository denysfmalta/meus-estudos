// OPERADOR TERNÁRIO

/*
var resultado = (condição) ? valor1 : valor2
*/

/* let enviar_email = true
let frase = enviarl_email ? "Enviar" : "Não enviar"
console.log(frase) */

// -----------------

/* let valor = 10
let frase = valor < 5 ? 'SIM' : 'NAO'
console.log(frase) */

// ---------------
let valor = 100
valor == 10 ? console.log('SIM') : console.log('NAO')
